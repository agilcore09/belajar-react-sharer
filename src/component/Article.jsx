function Article(props)
{
    return (
        <>
        <div><h1>{props.name}</h1></div>
        <div>
            {props.title.map((titles) => {
                return 
            })}
        </div>
        </>
    )
}
export default Article; 